EC2란?

AWS의 EC2를 이용해 LINUX 사용하기 
설치 :
ec2클릭 -> 빠른시작에서  SUSE Linux Enterprise Server 15 SP1 (HVM), SSD Volume Type - ami-0e29958d5b090434f 누르고 
프리티어 누르기 ->키설정: 키가 있어야만 로그인이 된다.(aws를 쓰니까 우리는 aws로 이름을 지음)->aws.pem폴더가 생성 
->puttygen 눌러서 key 등록하면 aws.ppk파일이 생성 putty 프로그램 실행시켜서 connection ssh auth에 키불러오고 
session에서 loggin에 aws사이트에 내게 주어지는 IPV4 퍼블릭 IP를 등록해서 키게 되면 실행이된다.
